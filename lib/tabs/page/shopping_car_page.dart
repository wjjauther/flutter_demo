import 'package:flutter/material.dart';

class ShoppingCarItemPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(
      child: Text(
        "购物车页面",
        style: TextStyle(color: Colors.red, fontSize: 20.0),
      ),
    );
  }
}
